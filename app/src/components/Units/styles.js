import { makeStyles } from '@mui/styles';

export default makeStyles({
    mainContainer: {
        display: 'flex',
        alignItems: 'center',
    },
    actionDiv: {
        textAlign: 'center',
    }
});